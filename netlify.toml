[build]
  command = "npm run build"
  publish = "dist"

[[edge_functions]]
  path = "/api/card-image"
  function = "card-image"

# SPA fallback - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
